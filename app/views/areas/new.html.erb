<div class="mx-auto md:w-2/3 w-full">
  <h1 class="font-bold text-4xl">New area</h1>

  <%= render "form", area: @area %>

  <%= link_to 'Back to areas', areas_path, class: "ml-2 #{style_btn(variant: :neutral)}" %>
</div>
