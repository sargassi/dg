    <% if section.present? %>
        <section class="bg-accent-50 dark:bg-accent-900/30 my-0">
        <div class="mx-auto section-qr">
            <div id="qr-reader">

            </div>
            </div>
        </section>

    <% end %>
<section class="<%= style_import_form %> rounded shadow-sm" >
  <div class="<%= style_search_container %> mt-24">
  <%= search_form_for @q, url: app_check_single_qr_path(:place => params[:place]), data:{turbo_frame: 'success'} do |f| %>
  <% if section.present? %>
      <%= f.search_field :qr_eq, class: style_search_input, placeholder: 'QR Code' %>
  <% else %>
    <%= f.search_field :qr_eq, class: style_search_input, placeholder: 'Selezionare prima una zona', disabled: true %>

  <% end %>
        <%= hidden_field_tag :place, nil, :value => params[:place] %>
        <%= button_tag(:class => style_main_btn_icon + " hidden", :id => 'btin') do %>
          <span class="material-symbols-outlined font-bold">
          qr_code_scanner
          </span>
          <% end %>
    <% end %>

</div>

</section>

<section class="<%= style_import_form %> rounded shadow-sm" >
  <div class="<%= style_search_container %> mb-24 my-8">
    <ul class="flex gap-4 w-full justify-center">
    <% @sections.each do |sec| %>
      <li>
        <a href="<%= "/app/sez?place=#{sec}" %>" class="<%= style_section_btn(active: (params[:place].present? && params[:place] != '' && params[:place] == sec)) %>">
          <%= sec %>
        </a>
      </li>
    <% end %>
    </ul>
  </div>
</section>
