<% content_for :page_header do %>
<div class="flex items-center gap-2 px-0 py-0.5">
  <h1 class="text-sm font-semibold text-slate-500 dark:text-slate-400 tracking-wide">
    ok'am srl
  </h1>
</div>
<% end %>

<section class="bg-white rounded-none p-4">
  <% if current_user.godlike? || current_user.has_role?(:ufficio) %>
    <%= render partial: 'dashboard/atoms/ufficio' %>
  <% elsif current_user.has_role?(:lab) %>
    <%= render partial: 'dashboard/atoms/lab' %>
  <% end %>
</section>
