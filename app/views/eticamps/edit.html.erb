<div class="mx-auto md:w-2/3 w-full">
  <h1 class="font-bold text-4xl">Editing eticamp</h1>

  <%= render "form", eticamp: @eticamp %>

  <%= link_to "Show this eticamp", @eticamp, class: "ml-2 #{style_btn(variant: :neutral)}" %>
  <%= link_to "Back to eticamps", eticamps_path, class: "ml-2 #{style_btn(variant: :neutral)}" %>
</div>
