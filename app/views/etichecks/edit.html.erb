<% content_for :title, "Editing eticheck" %>

<div class="md:w-2/3 w-full">
  <h1 class="font-bold text-4xl">Editing eticheck</h1>

  <%= render "form", eticheck: @eticheck %>

  <%= link_to "Show this eticheck", @eticheck, class: "ml-2 #{style_btn(variant: :neutral)}" %>
  <%= link_to "Back to etichecks", etichecks_path, class: "ml-2 #{style_btn(variant: :neutral)}" %>
</div>
