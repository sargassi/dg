<% pdf_grid_overlay %>
<div id="lab">
        <% counter = 0 %>
        <% if counter == 0 || counter.modulo(24) == 0 %>
            <div class="mm2">&nbsp;</div>
        <% end %>
        <% i = 0 %>
       <% @products.each do |iz, val| %>
        <% if i == 0 %>
          <% val.sort.each do |product| %>
             <% qty = product.qt.to_i %>
             <% cqty  = 0 %>
              <% qty.times do %>
                 <% cqty = cqty + 1 %>
                  <div class="cell sm" style="padding: 2mm 4mm">
                    <div class="sub" style="height:32mm;">
                    <p class="sm" style="border-bottom: 1px dashed #ccc; padding-bottom:.35mm; font-size: 8pt!important; height: 13mm; line-height: 9pt!important">
                        <span style="font-weight:bold;">articolo:</span>
                        <span style="margin-right:2mm">
                        <%= product.itemcode %> <%= product.fabricode %> <%= product.varcode %> </span>


                        <br>


                      <%= raw(product.description.gsub(/\R+/, '<br/>')) %> <%= product.materiale %> <%= product.fabric  %>
                   tg <%= product.tg %></p>


                <p class="sm" style="border-bottom: 1px dashed #ccc; padding-bottom:.35mm; padding-top:.5mm; font-size: 8pt!important">
                <span style="font-weight:bold; margin-right: 1mm">cliente:</span><%= product.dove %> <span style="font-weight:bold;"> <%= cqty %> / <%= qty %></span>

                </p>

                <p class="sm" style="border-bottom: 1px dashed #ccc; padding-bottom:.35mm; padding-top:.5mm; font-size: 8pt!important">
                <span style="font-weight:bold; margin-right: 1mm">tagliato da:</span>
                </p>
                <p class="sm" style="border-bottom: 1px dashed #ccc; padding-bottom:.35mm; padding-top:.5mm; font-size: 8pt!important">
                <span style="font-weight:bold; margin-right: 1mm">cucito da:</span>
                </p>
                <p class="sm" style="border-bottom: 1px dashed #ccc; padding-bottom:.35mm; padding-top:.5mm; font-size: 8pt!important">
                <span style="font-weight:bold; margin-right: 1mm">lavato da:</span>
                </p>
                <p class="sm" style="border-bottom: 1px dashed #ccc; padding-bottom:.35mm; padding-top:.5mm; font-size: 8pt!important">
                <span style="font-weight:bold; margin-right: 1mm">controllato da:</span>
                </p>

            </div>

                <% counter = counter + 1 %>
                </div>
              <% if counter.modulo(24) == 0 %>
              <div style="clear:both; page-break-after: always;"></div>
              <% end %>
            <% end %>

          <% end %>

        <% i += 1 %>
    <% end %>
    <% end %>
</div>
