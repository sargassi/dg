<div class="mx-auto md:w-2/3 w-full">
  <h1 class="font-bold text-4xl">New etigen</h1>

  <%= render "form", etigen: @etigen %>

  <%= link_to "Back to etigens", etigens_path, class: "ml-2 #{style_btn(variant: :neutral)}" %>
</div>
