<div class="mx-auto md:w-2/3 w-full">
  <h1 class="font-bold text-4xl">Editing eventype</h1>

  <%= render "form", eventype: @eventype %>

  <%= link_to "Show this eventype", @eventype, class: "ml-2 #{style_btn(variant: :neutral)}" %>
  <%= link_to "Back to eventypes", eventypes_path, class: "ml-2 #{style_btn(variant: :neutral)}" %>
</div>
