<div class="mx-auto md:w-2/3 w-full">
  <h1 class="font-bold text-4xl">Editing fabriclu</h1>

  <%= render "form", fabriclu: @fabriclu %>

  <%= link_to "Show this fabriclu", @fabriclu, class: "ml-2 #{style_btn(variant: :neutral)}" %>
  <%= link_to "Back to fabriclus", fabriclus_path, class: "ml-2 #{style_btn(variant: :neutral)}" %>
</div>
