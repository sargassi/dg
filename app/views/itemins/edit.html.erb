<div class="mx-auto md:w-2/3 w-full">
  <h1 class="font-bold text-4xl">Editing itemin</h1>

  <%= render "form", itemin: @itemin %>

  <%= link_to "Show this itemin", @itemin, class: "ml-2 #{style_btn(variant: :neutral)}" %>
  <%= link_to "Back to itemins", itemins_path, class: "ml-2 #{style_btn(variant: :neutral)}" %>
</div>
