<div class="mx-auto md:w-2/3 w-full">
  <h1 class="font-bold text-4xl">Editing itemout</h1>

  <%= render "form", itemout: @itemout %>

  <%= link_to "Show this itemout", @itemout, class: "ml-2 #{style_btn(variant: :neutral)}" %>
  <%= link_to "Back to itemouts", itemouts_path, class: "ml-2 #{style_btn(variant: :neutral)}" %>
</div>
