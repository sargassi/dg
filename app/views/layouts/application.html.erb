<!DOCTYPE html>
<html>
  <head>
    <title>Ok'am Srl</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Material+Symbols+Outlined:opsz,wght,FILL,GRAD@24,100,0,-25" >
    <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>

    <% if controller_name == 'app' %>
      <%= render partial: 'layouts/headers/app' %>
      <%= turbo_frame_tag 'success' %>
      <% if notice %>

        <p id="notice" class="bg-green-400 p-4 w-full text-white absolute bottom-0 left-0 delay-300" data-controller="notifications"><%= notice %></p>

      <% end %>

      <%= yield %>

    <% else %>
        <main class="flex gap-4">
            <section class="w-16 fixed top-0 left-0 h-screen">
            <%= render partial: '/layouts/menus/side/structure' %>
            </section>

            <section class="home px-4 my-2 ml-16 grow">
            <%= render partial: 'layouts/menus/top/structure' %>
            <%= turbo_frame_tag 'success' %>
            <%= yield %>
            </section>
        </main>

    <% end %>

  </body>
</html>
