<header>
 <% if current_user.present? %>
   <% if action_name != 'dashboard' %>
  <div class="flex gap-8 justify-between items-center w-full text-muted text-center text-sm py-2 my-16">
   <p class="w-40">
     <a href="/app/dashboard">
       <span class="material-symbols-outlined">arrow_back_ios</span>
     </a>
   </p>
   <p class="grow textcenter font-semibold">
     <%= @title %>
   </p>
   <p class="w-40">
   </p>
  </div>
  <% end %>
  <% end %>
</header>
