<div class="mx-auto md:w-2/3 w-full">
  <h1 class="font-bold text-4xl">Editing proforma</h1>

  <%= render "form", proforma: @proforma %>

  <%= link_to "Show this proforma", production_proforma_path(@proforma), class: "ml-2 #{style_btn(variant: :neutral)}" %>
  <%= link_to "Back to proformas", production_proformas_path, class: "ml-2 #{style_btn(variant: :neutral)}" %>
</div>
