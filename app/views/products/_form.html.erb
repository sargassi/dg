    <%= turbo_frame_tag :description do%>

<%= form_with(model: product, class: "contents", data: {'controller': 'populate'}) do |form| %>
  <% if product.errors.any? %>
    <div id="error_explanation" class="bg-red-50 text-red-500 px-3 py-2 font-medium rounded-lg mt-3">
      <h2><%= pluralize(product.errors.count, "error") %> prohibited this product from being saved:</h2>

      <ul>
        <% product.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>


    <%= form.text_area :description, rows:2, class: "w-full p-0 m-0 leading-tight", wrap:'hard' %>


  


    <%= form.submit class:"no-pdf text-end text-xs uppercase absolute top-0 right-0" %>

<% end %>

    <% end %>