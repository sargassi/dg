<%= render partial: 'atoms/header', locals: {link: '/proformas', label: "Proforma / #{@proforma.id} del #{@proforma.data_in.strftime('%d/%m/%Y')}"} %>

<%= render @proforma %>

<%= render partial: 'atoms/actions', locals: {target: @proforma, var: 'proformas', redir: 'proformas', pdf: 'prof'} %>

<%= render partial: 'prows/rows', locals: {rows: @rows} %>

<%= render partial: 'atoms/ux/modal' %>
