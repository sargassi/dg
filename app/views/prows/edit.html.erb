<div class="mx-auto md:w-2/3 w-full">
  <h1 class="font-bold text-4xl">Editing prow</h1>

  <%= render "form", prow: @prow %>

  <%= link_to "Show this prow", @prow, class: "ml-2 #{style_btn(variant: :neutral)}" %>
  <%= link_to "Back to prows", prows_path, class: "ml-2 #{style_btn(variant: :neutral)}" %>
</div>
