<div class="mx-auto md:w-2/3 w-full">
  <h1 class="font-bold text-4xl">Editing rail</h1>

  <%= render "form", rail: @rail %>

  <%= link_to "Show this rail", @rail, class: "ml-2 #{style_btn(variant: :neutral)}" %>
  <%= link_to "Back to rails", rails_path, class: "ml-2 #{style_btn(variant: :neutral)}" %>
</div>
