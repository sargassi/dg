<div class="mx-auto md:w-2/3 w-full">
  <h1 class="font-bold text-4xl">Editing station</h1>

  <%= render "form", station: @station %>

  <%= link_to "Show this station", @station, class: "ml-2 #{style_btn(variant: :neutral)}" %>
  <%= link_to "Back to stations", stations_path, class: "ml-2 #{style_btn(variant: :neutral)}" %>
</div>
