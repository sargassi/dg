<%= form_with(model: taglium, class: "contents") do |form| %>
  <% if taglium.errors.any? %>
    <div id="error_explanation" class="bg-red-50 text-red-500 px-3 py-2 font-medium rounded-lg mt-3">
      <h2><%= pluralize(taglium.errors.count, "error") %> prohibited this taglium from being saved:</h2>

      <ul>
        <% taglium.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="my-5">
    <%= form.label :description %>
    <%= form.text_field :description, class: "block shadow rounded-sm border border-slate-200 outline-none px-3 py-2 mt-2 w-full" %>
  </div>

  <div class="inline">
    <%= form.submit class: "#{style_btn(variant: :success)}" %>
  </div>
<% end %>
