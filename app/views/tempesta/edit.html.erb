<div class="mx-auto md:w-2/3 w-full">
  <h1 class="font-bold text-4xl">Editing tempestum</h1>

  <%= render "form", tempestum: @tempestum %>

  <%= link_to "Show this tempestum", @tempestum, class: "ml-2 #{style_btn(variant: :neutral)}" %>
  <%= link_to "Back to tempesta", tempesta_path, class: "ml-2 #{style_btn(variant: :neutral)}" %>
</div>
