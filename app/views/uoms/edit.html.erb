<div class="mx-auto md:w-2/3 w-full">
  <h1 class="font-bold text-4xl">Editing uom</h1>

  <%= render "form", uom: @uom %>

  <%= link_to "Show this uom", @uom, class: "ml-2 #{style_btn(variant: :neutral)}" %>
  <%= link_to "Back to uoms", uoms_path, class: "ml-2 #{style_btn(variant: :neutral)}" %>
</div>
