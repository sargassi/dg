<%= turbo_frame_tag "fabric-#{product.id}" do  %>
  <div class="flex justify-between mb-2 border-b">
    <span class="leading-tight  block mr-4"><%= raw(product.fabric.gsub(/\R+/, '<br/>')) if !product.fabric.nil? %> </span> <%= link_to [:edit, product, attribute: :fabric ], class:"no-pdf text-end text-xs uppercase text-accent-600 font-bold" do %>

      <%= icon_edit %>

      <% end %>
  </div>
<% end %>
